# ── Banco de Dados (PostgreSQL) ──────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jobprospector
DB_USER=postgres
DB_PASSWORD=change-me

# ── PostgreSQL Docker (usado pelo docker-compose) ─────────────────────────────
POSTGRES_DB=jobprospector
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

# ── Hunter.io API ─────────────────────────────────────────────────────────────
# Obtenha sua chave gratuita em: https://hunter.io/api-keys
HUNTER_API_KEY=your_hunter_api_key_here

# ── E-mail SMTP ───────────────────────────────────────────────────────────────
# Gmail: use uma App Password (não sua senha pessoal)
# Gere em: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_EMAIL=your@email.com
SMTP_SENDER_PASSWORD=your_app_password_here
SMTP_SENDER_NAME=Job Prospector
